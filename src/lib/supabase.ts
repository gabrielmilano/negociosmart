// Re-export the single Supabase client to avoid multiple instances
export { supabase } from '@/integrations/supabase/client'
export type { Database } from '@/integrations/supabase/database.custom.types'
